from typing import Any

from tale.syntax import Node

def evaluate(tree: Node) -> Any:
    """Evaluates syntax tree node and returns a result.

    This method is used by the `core` module after successful parsing of syntax tree.

    Args:
        tree: A root node of syntax tree that represents the program.
    """

    return ''
